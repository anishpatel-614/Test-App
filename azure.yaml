name: mortgage-calculator
infra:
  provider: bicep
  path: infra
services:
  web:
    project: .
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile
      context: .
      remoteBuild: true
